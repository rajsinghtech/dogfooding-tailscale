apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: goldpinger
  namespace: argocd
spec:
  destination:
    namespace: goldpinger

    server: https://kubernetes.default.svc
  project: default
  source:
    path: clusters/cluster1-aws-eastus/goldpinger

    repoURL: https://github.com/rajsinghtech/dogfooding-tailscale
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
      - CreateNamespace=true