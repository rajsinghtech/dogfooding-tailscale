apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd
  namespace: argocd
spec:
  defaultBackend:
    service:
      name: argo-cd-argocd-server
      port:
        number: 8080
  ingressClassName: tailscale
  tls:
    - hosts:
        - argocd