---
apiVersion: tailscale.com/v1alpha1
kind: Connector
metadata:
  name: ${CLUSTER_NAME}-connector
  namespace: tailscale
spec:
  proxyClass: prod
  hostname: ${CONNECTOR_HOSTNAME}
  subnetRouter:
    advertiseRoutes:
      - "${CONNECTOR_CIDR}"
